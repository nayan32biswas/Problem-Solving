#include <bits/stdc++.h>
using namespace std;
#define PI acos(-1)
#define vol(h, r) (h*PI*r*r)
bool same(double A, double B){
    return abs(A-B)<0.0000001;
}
double D, d, H;
bool binary(double key){
    double start = 0.000001, End = (D/2), mid, inc=000001, ;
    while(start<End){
        mid = (start+End)/2;
        
    }
}
int main()
{
    int test;
    
    scanf("%d", &test);
    for(int t=1; t<=test; t++){
        scanf("%lf%lf%lf", &D, &d, &H);

        puts(binary((4.0/3)*PI*r*r*r) ? "Yes" : "No");
    }
    return 0;
}